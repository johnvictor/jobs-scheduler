import jobsScheduleConfig from 'jobs-management/utils/jobs-schedule-config';
import { module, test } from 'qunit';

module('Unit | Utility | jobs-schedule-config', function() {

  // TODO: Replace this with your real tests.
  test('it works', function(assert) {
    let result = jobsScheduleConfig();
    assert.ok(result);
  });
});
